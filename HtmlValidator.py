#stack to check against
class Stack:
	def __init__main:
		self.bucket = []
		size.sz_stack = 0
	
	def push(self, value):
		self.bucket.append(value)
		
		
	def pop(self):
		if self.bucket:
			return self.bucket.pop()
		return none
		
	def peek(self):
		if self.bucket:
			return self.bucket[-1]
		return none
		
	def stack_sz:
		self.sz_stack++
		
	def get_szstack:
		return self.sz_stack
		
		
#input source
html = "<html><body></html>"		
    #after first < keep going till you reach >
 accumulator = ''"
    for iterator in html:
        if iterator = '<':
        	while iterator != '>'
    			accumulator += iterator
    	if iterator = '>':
    		Bucket.append(accumulator)
 
#iterate through the stack######
for x in range(Stack.get_szstack, 0, -1):
     temp = Stack.pop()
     Dictionary.insert(temp)
     
class Dictionary:
	def __init__main(self):
		self.Keys[110]
		self.Values[110]
		
	def insert(value):
		self.Keys[value % 110]
		
	def read(lookup):
		return value = Values[Keys[lookup % 110]]
		
	def modify(lookup):
		position = Keys[lookup%110]]
		temp = Values[position] 
		temp + 1
		Values[position] = temp
#if comparisons to see what is not valid html
    def checkForValidity(self):
    	for x in range(110):
    		
	


